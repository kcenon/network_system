# Network System Environment Configuration
# Copy this file to .env and set the paths to your local installations

# Optional: Set paths to system dependencies
# If not set, CMake will search in sibling directories and system paths

# Common system (provides Result<T> pattern)
# export COMMON_SYSTEM_ROOT=/path/to/common_system

# Thread system (provides thread pool)
# export THREAD_SYSTEM_ROOT=/path/to/thread_system

# Logger system (provides logging integration)
# export LOGGER_SYSTEM_ROOT=/path/to/logger_system

# Container system (provides message containers)
# export CONTAINER_SYSTEM_ROOT=/path/to/container_system

# Example for typical development setup:
# export COMMON_SYSTEM_ROOT=../common_system
# export THREAD_SYSTEM_ROOT=../thread_system
# export LOGGER_SYSTEM_ROOT=../logger_system
# export CONTAINER_SYSTEM_ROOT=../container_system
