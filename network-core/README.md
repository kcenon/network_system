# network-core (legacy staging location)

> **Note**: The active `network-core` library is maintained at [`libs/network-core/`](../libs/network-core/README.md).
> The top-level `network-core/` directory remains only for in-tree header inclusion compatibility
> (see `include` paths referenced in the top-level `CMakeLists.txt`). All new documentation
> and usage instructions live in [libs/network-core/README.md](../libs/network-core/README.md).

<!-- TODO: Remove this directory once in-tree consumers are migrated to libs/network-core/include -->
